//! The two navigation rails (`docs/ux/desktop/Shell & Navigation/README.md`'s "1a" spec):
//! the primary rail (every noun) and the context rail (scoped to the active noun).

pub mod context;
pub mod primary;
