//! View interiors, one module per noun that has one built. Dashboard (issue #148) and Settings
//! (issue #173) exist so far -- the rest are placeholder views, a separate ticket (#153).

pub mod dashboard;
pub mod settings;
